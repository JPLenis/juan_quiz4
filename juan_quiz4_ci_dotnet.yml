trigger:
- main

pool:
  name: 'Default'  

steps:
- task: UseDotNet@2
  inputs:
    packageType: 'sdk'
    version: '8.0.408'
    installationPath: $(Agent.ToolsDirectory)/dotnet

- script: dotnet restore juan_quiz4.csproj
  workingDirectory: '.'
  displayName: 'Restore packages'

- script: dotnet build juan_quiz4.csproj --configuration Release
  workingDirectory: '.'
  displayName: 'Build'

- script: dotnet publish juan_quiz4.csproj -c Release -o $(Build.ArtifactStagingDirectory)
  workingDirectory: '.'
  displayName: 'Publish'

- task: PublishBuildArtifacts@1
  inputs:
    PathtoPublish: '$(Build.ArtifactStagingDirectory)'
    ArtifactName: 'drop'
    publishLocation: 'Container'
